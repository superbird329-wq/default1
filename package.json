{
  "name": "vincataldo-com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "strip-metadata": "./scripts/strip-metadata.sh",
    "check:todos": "./scripts/check-todos.sh",
    "check:confidential": "./scripts/check-confidential.sh",
    "verify": "npm run check && npm run check:contrast && npm run build && npm run check:todos && npm run check:confidential",
    "check:contrast": "node scripts/check-contrast.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.9",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
